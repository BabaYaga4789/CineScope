import {
    Alert,
    AlertIcon,
    AlertTitle,
    AlertDescription,
    Box,
    AlertDialog,
  } from "@chakra-ui/react";
import { useRef } from "react";

export const AlertDialogForDeleteConfirm = () => {
    // Reference: https://chakra-ui.com/docs/components/alert
    const cancelRef = useRef<HTMLButtonElement>(null);

    return(
        
    )
}